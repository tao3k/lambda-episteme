# Pinned external FHIR payloads are source evidence. Preserve their exact
# upstream bytes so Sources Lock digests survive clean checkouts on every host.
modules/healthcare/standards/fhir/sources/**/*.json -text
