#+title: POO contribution module contract

* Framework-owned module contract
The normative layout is documented by POO Flow in
=docs/10-19-design/10.06-poo-module-system/contribution-module-layout.org=.
Each module follows =types.ss=, =objects.ss=, =funs.ss=, =config.ss= and
=interface.ss=. This repository does not implement its own layout checker,
Gerbil package, source registry, build script, or =modules/init.ss=. POO Flow's
trusted contribution registry maps =lambda-episteme= and its public module
names to this repository's pinned source. POO Flow's =poo-flow-load-modules=
discovers direct =modules/<module-name>/= directories through their sole public
=interface.ss= entrypoints and constructs the
contributor and user source collections from repository roots. Its root-owned
contribution adapter projects those directories to one
ASP source stage per =modules/<name>/= tree. The shared deterministic tree walk
automatically supplies that module's Scheme files. No downstream =modules=
override, =all-gerbil-modules= call, contribution-owned source object, compiler,
scheduler, or cache is supplied. Imported helpers remain native =std/make=
dependencies and never need to be repeated in the build declaration.

* NASA coverage
NASA NPR 7150.2D now has a 130-identity catalog: 30 Chapter 2 institutional
requirements and 100 Appendix C project matrix rows. Each row preserves edition,
section, source URL, a source excerpt and an unevaluated assessment. Matrix rows
retain A-F invocation and both authority columns. Chapter 2 applicability remains
in the requirement text. Blank matrix cells mean not invoked by that matrix;
they do not grant a waiver or establish final project applicability.

The source snapshots and SHA-256 values accompany the catalog. Regenerate with:
#+begin_src sh
python3 tools/import-nasa-catalog.py \
  modules/sdlc/standards/sources \
  modules/sdlc/standards/nasa-7150-2d-catalog.ss
#+end_src
The authoring tool requires BeautifulSoup. Python is not a Scheme build or
runtime dependency. Authoritative Chapters 2–5 now supply 255 paragraph-level
review criteria for all 130 identifiers; Appendix C supplies class/authority
routing. Notes remain separate. The catalog uses a shared POO constructor over
quoted data, avoiding one macro expansion per source row during native builds.
External standards, authentic signatures, and substantive human review remain
outside automatic compliance claims. Chapter 6 is retained as a source snapshot
for documentation guidance; it does not create additional SWE requirements.

* Qualification on 2026-09-12
The POO Flow-owned directory build compiled 25 discovered module/support sources.
The independent user-scenario build also passed. Governance, SDLC, config,
user-scenario and NASA-catalog suites passed 18, 17, 7, 39 and 268 checks
respectively (349 total). The catalogue regenerated identically from the saved
source snapshots. This validates source modeling and review preparation only.

* Module-owned tests
Executable tests live under =t/<owner>/<lane>/= and declare/export ordinary
=:std/test= suites with the same binding as the file basename. The single =unit-tests.ss= entry delegates
discovery and execution to =:clan/testing=.
Core policy and macro documentation live in POO Flow's
=contribution-test-ownership-rfc.org=, not a child-owned testing framework.

* POO Flow contribution build integration
POO Flow owns the default core PackageSpec, package namespace, root-level
contribution adapter, =std/make= handoff, and build environment. Contribution
production and test stages are independent from the core public closure but
install into the shared namespace. There is no contribution-owned package
manager, build script, compiler, scheduler, or artifact cache.
