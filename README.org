#+title: Lambda Episteme — vertical knowledge as POO composition

* Purpose

Lambda Episteme is the vertical-industry contribution field for POO Flow.  It
turns domain concepts, relations, policies, sources and concrete situations
into native Gerbil POO values that applications can select and compose.

This repository owns vertical-domain meaning, governance content and its
qualification evidence.  POO Flow owns the Governance core contract, module
system, composition engine, graph implementation, build API and runtime
authorization handoff.  Mounting this repository does not activate a Profile,
grant a capability or authorize a decision; the consuming application must
explicitly select the composition it accepts.

The public model is:

#+begin_example
Module     semantic mechanism and stable public API
  |
Scenario  one vertical-industry boundary
  |
Profile   reusable, inheritable domain configuration
  |
Case      concrete composition plus declarative graph facts
  |
Source    explicit common or Case-local external authority
#+end_example

These are different ownership levels, not interchangeable names.  For example,
=ontology= is a Module; =healthcare= is a Scenario; =healing= is a Profile; and
=post-operative-healing= is a Case.

Governance itself is not another Lambda top-level framework.  Lambda Profiles
derive from POO Flow's =PooFlowGovernanceProfile.= and declare their contributor
owner and revision explicitly.  POO Flow's Cedar bridge may project an admitted
Profile into an identity-bound authority snapshot; only the independent
Rust+Lean Runtime Host evaluates that snapshot.  The Provider lives under POO
Flow's core =src/modules/authorization/providers/cedar/= module rather than the
legacy generic =policy= namespace.  Domain admission, Cedar input
projection and runtime authorization are three separate receipts.

* Security Governance evidence chain

Vertical threat knowledge belongs beside the Scenario and Profile that gives
it meaning.  Lambda therefore declares Healthcare, finance, manufacturing or
other domain threats through the POO Flow Governance slots; it does not create
a second generic Governance module.  For example, the Healthcare medication
safety Profile declares a critical contraindication-evidence threat through
=threat-declarations=.  That slot is projected to the core =ThreatModel= automatically,
and every selected Profile is assessed while its Case composition receipt is
built.

#+begin_src scheme
(.def (MedicationSafetyProfile @ HealthcareBaseProfile)
  ;; ontology declarations omitted
  (threat-declarations =>.+
   (.o contraindication-evidence-drift:
       (poo-flow-governance-threat
        "healthcare/medication/threat/contraindication-evidence-drift"
        'critical
        'mitigated
        (list
         (poo-flow-governance-precondition
          "healthcare/medication/precondition/reconciliation-observed"
          #t
          "evidence:post-operative-medication-reconciliation"))
        mitigations:
        '("medication reconciliation bound to encounter evidence")))))
#+end_src

This remains declarative user configuration: Profile authors do not maintain a
parallel threat list or assessment registry.  Standard Cedar policy/schema
Sources stay beside the Healthcare Scenario, while the Case declares only its
Provider, Patient, Encounter and Medication Order slots.  The Case receipt
contains =governance-assessments= and =governance-handoff-ready?=.  Replacing
the threat phase with =exposed= or =realized= in a derived Profile makes the
Case fail closed with =governance-handoff-blocked= and the exact unresolved
threat identity.

The complete assurance path has distinct owners:

1. Lambda owns vertical Scenario/Profile/Case meaning and explicit Source
   provenance.
2. POO Flow owns typed threat admission and inert handoff-readiness receipts.
3. TLA+ with official TLC checks the Governance transition system and emits a
   digest-bound model receipt.
4. Lean proves the general enterprise obligations, including typed security
   graph closure, evidence identity, separation of duties, and the theorem
   that an active critical threat prevents Provider handoff.
5. Cedar receives only an admitted, identity-bound snapshot; the independent
   Runtime Host remains the authorization and execution boundary.

The exact AI-assisted prescription qualification declares its GQL, TLA+, TLC
receipt and Lean receipt/proof identities in one POO
=HealthcareCaseQualificationMetadata= object.  Just invokes the independent
capabilities but does not reconstruct those semantics with path environment
variables.  GQL execution is intentionally outside this safety closure: a
future MRR Rust library consumes gerbil-parser's native FFI and must publish its
own execution receipt.

No step collapses into the next.  A Lambda Case does not grant authority, a
TLC run is not a Lean theorem, a Lean theorem is not a current Cedar decision,
and Cedar cannot bypass an unresolved Governance threat.  This separation is
the basis for high-assurance enterprise use rather than an implementation
detail.

* Repository shape

#+begin_example
modules/
  ontology/                  semantic mechanism
  diataxis/                  knowledge-document classification
  decision-kind/             decision-kind knowledge model

user-interface/
  init.ss                    selected Modules and feature flags
  config.ss                  final compositions only
  profiles/                  reusable cross-Scenario Profiles
  scenarios/
    <vertical>/
      scenario.ss            vertical boundary and base Profile
      profiles/              vertical reusable Profiles
      cases/<case>/case.ss   one concrete declaration
#+end_example

Every contributed Module follows the POO Flow-owned five-role contract:
=types.ss=, =objects.ss=, =funs.ss=, =config.ss= and =interface.ss=.  See
[[file:MODULES.org][MODULES.org]] for the normative ownership of those files.

The currently migrated ontology Scenarios are Healthcare, Software
Engineering, Commercial Finance, Manufacturing and Education.  They all reuse
the same =modules/ontology= mechanism; no industry creates a second ontology
engine or module registry.

Healthcare's concrete enterprise relation model, derived Impact analysis and
separation between GQL, TLA+/TLC, Lean and Cedar are documented in
[[file:user-interface/scenarios/healthcare/README.org][Healthcare Scenario relations and assurance flow]].

* Declarative user interface

The User Interface exposes concrete POO declarations.  A contributor composes
objects through named slots instead of maintaining raw alists, parallel
registries, stage DSLs or procedural setup code.

** Profile

A Profile inherits an existing prototype and declares only its additions:

#+begin_src scheme
(.def (HealingProfile @ HealthcareBaseProfile)
  (identity "lambda-episteme/ontology/healthcare/healing")
  (name 'healing)
  (profile-imports =>.+ (.o healthcare: HealthcareBaseProfile
               evidence: EvidenceProfile))
  (concept-declarations =>.+
   (.o healing-episode:
       (ontology-concept 'HealingEpisode '(Encounter) '(time-bound))))
  (relation-declarations =>.+
   (.o recovers-from:
       (ontology-relation
        'RECOVERS_FROM 'HealingEpisode 'Condition '())))
  (source-declarations =>.+ (.o))
  (rule-declarations =>.+ (.o))
  (query-declarations =>.+ (.o healing-progress: 'healing-progress)))
#+end_src

Top-level =user-interface/profiles/= contains reusable common Profiles.
=user-interface/scenarios/<vertical>/profiles/= contains Profiles owned by one
vertical Scenario.  Inheritance and =profile-imports= composition make reuse explicit;
filesystem placement alone never activates a Profile.

** Case

A Case is one POO declaration.  It selects Profiles with =profile-selection=,
adds Case-local Sources with =source-declarations= when needed, and declares graph
topology through the Graph prototype's =node-declarations= and =edge-declarations= slots:

#+begin_src scheme
(.def (TransactionPostingCase @ OntologyCase)
  (case-id 'transaction-posting)
  (scenario CommercialFinanceScenario)
  (profile-selection =>.+
   (.o commercial-finance:
       (.o evidence: EvidenceProfile
           privacy: PrivacyProfile
           base: CommercialFinanceBaseProfile)))
  (graph
   (.o (:: @ Graph)
       graph-id: 'transaction-posting
       node-declarations:
       (.o customer-1:
           (poo-flow-graph-node 'customer-1 'Customer))
       edge-declarations: (.o))))
#+end_src

=node-declarations= and =edge-declarations= are real POO slots, not lists disguised as a graph
DSL.  The Graph owner derives the projections used by algorithms.  Likewise,
the framework derives composition sequences, source sequences, indexes,
admission results and evaluations; Case authors do not duplicate those values
as sibling =def= bindings.

** Sources

Sources follow the same scope model as Profiles:

- a reusable Profile may declare common Sources in =source-declarations=;
- a Scenario Profile may declare vertical Sources;
- a Case may add Sources that apply only to that concrete situation.

A Source is explicit authority metadata, not an implicit file scan.  Its scope
and ownership remain visible in the resulting composition.

Checked-in external bytes use POO Flow's shared Sources Lock Feature.  The
editable =sources.ss= declares identity, path, canonical URI, exact version and
representation; =source.lock.ss= is generated from those declarations and the
actual bytes.  Domain loaders consume the generated lock and attach its
verification receipt to the Standard artifact.  They do not repeat digest,
size, path or URI constants.

* Programming model

The implementation uses the smallest native abstraction that matches the
extension axis:

- POO prototypes and ordinary slots are the default data and composition model.
- =.defgeneric= slot dispatch owns behavior that varies along one Profile,
  Scenario or semantic-object axis.  A derived object replaces the method slot
  directly.
- CLOS generics, class specializers and method bundles are reserved for a real
  crossing of independently extensible axes, such as Provider/executor by
  Profile, Scenario or Case.
- =define-type= defines actual boundary contracts such as Git Change, Check and
  Decision.  It does not simulate a class hierarchy.
- =defrule= may remove repetition among Profile declarations.  It must expand
  to ordinary POO-native or functional code, not introduce another public DSL.
- =config.ss= selects and composes Profiles and Standards only.  It does not
  repeat execution stages.
- =interface.ss= is the sole public re-export boundary of a Module.

Pure closure, indexing, admission and projection functions belong in
=funs.ss=.  Side effects remain behind explicit runtime or application
boundaries.  Imports express source ownership; selective =only-in= imports are
used where they make dependencies precise, not as an invented substitute for
the build graph.

* Ontology without a second RDF authority

The native authority is the accepted POO composition: Profiles own concepts,
relations, rules, queries and Sources; Cases own concrete graph facts.  RDF is
therefore neither a dependency of =modules/ontology= nor a second mutable
semantic store.

If an external consumer requires RDF, an adapter may project RDF one way from
an accepted POO value at that consumer boundary.  Generated RDF must not be
read back to mutate or supersede the Profile and Case semantics.  The same rule
applies to optional GQL, SQL and registry artifacts: they are explicit Sources
or consumer projections, not hidden runtime owners.

For accepted Scenario/Case/Profile compositions, the Ontology module can
project the same POO objects into a POO Flow Graph.  GQL may query that
projection, while native reverse dependency analysis derives Impact receipts
for changed Profiles, Sources, Threats or Capabilities.  Impact is never a
parallel user-maintained registry.  Parser-qualified GQL source establishes
syntax and lossless authoring only; it does not claim query execution.

* Adding a contribution

** Add a vertical Scenario

1. Add =user-interface/scenarios/<vertical>/scenario.ss= and derive one base
   Profile from =OntologyProfile=.
2. Put vertical reusable Profiles in =profiles/=.  Import common Profiles from
   =user-interface/profiles/= explicitly.
3. Add each concrete situation as one
   =cases/<case-name>/case.ss= declaration.
4. Declare common, vertical or Case-local Sources at the scope that owns them.
5. Add native tests under =t/ontology/= for composition, admission, graph
   behavior and relevant negative cases.

** Add a Module

1. Start from =templates/module/= and implement the five required role files.
2. Keep domain behavior in the contribution; do not copy POO Flow's loader,
   builder, scheduler, testing framework or artifact cache.
3. Export only through the Module's =interface.ss=.
4. Add a runnable User Interface composition and module-owned native tests.

Detailed contribution rules are in [[file:CONTRIBUTING.org][CONTRIBUTING.org]].

* Build and test

=gerbil.pkg= declares the native package identity
=poo-flow/lambda-episteme=.  The sole =build.ss= is a declarative package entry
derived from ASP's library PackageSpec prototype; it does not implement a
compiler, scheduler, scanner or cache.  Its =justfile= delegates Scheme work to
POO Flow's official build and test adapters.

The mutable User Composition in =user-interface/config.ss= is not a production
package root.  Consumers and the atomic User Interface lane compile it on
demand, which keeps Case and Profile selection out of the reusable library
closure.

From this directory:

#+begin_src sh
just build-scheme
just test-scheme ontology
just test-scheme-atomic ontology unit/ontology-test.ss
just test-scheme-all
just update-fhir-sources-lock
#+end_src

Use =test-scheme= for one module tree, =test-scheme-atomic= for one exact test
file, and =test-scheme-all= for the complete acceptance suite.  The two focused
commands are native =gxtest= filters for local diagnosis.  The complete entry
is the authoritative profiled path: ordinary =:std/test= suites are discovered
by upstream =:clan/testing= and executed through the ASP Testing interface;
Lambda Episteme does not own a parallel runner.

Focused module and atomic commands allocate one ephemeral =GERBIL_PATH= per
invocation.  Compiled test dependencies therefore remain reusable by batches
inside that invocation but cannot shadow changed source in a later run.  A
module selector is first expanded by upstream =:clan/testing= into exact test
files; ASP then applies file-level Profiles and native concurrency.  Passing a
directory directly to one =gerbil test= process is not an accepted shortcut.
The complete suite uses the same ephemeral-path rule and emits a native Gerbil
=bootstrap-start= receipt before loading its profiled entry, so even cold
compilation is observable.  Testing and production build remain separate
commands: run the relevant test command before =just build-scheme=.

Testing extensions such as source admission, timeout, heap observation,
debugging and tracing are declarative ASP Testing Profiles around the native
gxtest lifecycle.  Every =*-test.ss= is mapped to ASP's process-isolation
Profile, so its heap limit applies to that file rather than a multi-file batch.
The source-admission suite additionally uses ASP's serial-resource Profile and
runs after the parallel lane. With =GERBIL_BUILD_VERBOSE=1=, the POO Flow
Observability extension emits
per-operation receipts and 5-second heartbeats for a stuck native batch.

The parent checkout owns locked dependencies, =std/make= handoff and build
parallelism.  =GERBIL_BUILD_CORES= is inherited by that build environment; this
repository does not calculate or override a competing core count.

* Ownership and limits

Lambda Episteme can model evidence, prepare reviews and expose unresolved
obligations.  It does not authenticate a producer, execute an external service,
certify regulatory compliance or authorize release by itself.  Those effects
belong to explicitly configured host Providers and independently tested
application boundaries.

ADR, GitOps and software-lifecycle assurance are owned and maintained by
Lambda Aitia.  Lambda Episteme does not retain compatibility modules, copied
qualification receipts or a second namespace for those semantics.

The former RDF registry, SQL policy, Python alignment and Wendao command
surfaces have been removed.  Their retained domain semantics now live only in
POO-native Modules, Profiles, Scenarios and Cases; external evidence is bound
through explicitly owned Scenario sources.

For the closed user-facing directory contract, see
[[file:user-interface/README.org][user-interface/README.org]].
